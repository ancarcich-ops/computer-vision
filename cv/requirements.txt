supervision==0.28.0
opencv-python>=4.8
numpy>=1.24

# Optional: only needed for the YOLO backend (--detector yolo) and training.
# Pulls in torch; CPU works (GPU auto-used if present).
ultralytics>=8.3

# Optional: H.264 re-encode of output videos for browser/mobile playback.
# (supervision writes mp4v, which many players can't decode.)
imageio-ffmpeg>=0.4
